attributes:
  # Show vpnaas only for neutron
  metadata:
    restrictions:
      - condition: cluster:net_provider != 'neutron'
        action: hide
      - condition: settings:neutron_advanced_configuration.neutron_dvr.value != false
        action: hide
